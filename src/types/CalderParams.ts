export type CalderParams = {
    HINGE_RADIUS: number, // The radius of hinge connector
    HINGE_NUMBER: number, // The number of rings to include when connecting mobiles
    DELTA_Y: number, // The shift in height to ensure the center of mass is below the write
}