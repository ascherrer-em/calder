import { CalderParams } from "../types/CalderParams";
import { GeometryService } from "./GeometryService";

export class CalderMobileService {

    constructor(public geometryService: GeometryService, public params: CalderParams) {}
    generateRandomMobile(){
        
    }

    testGenerateRandomNode(){
        
    }
}